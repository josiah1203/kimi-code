import type { SpiderByteHostIdentity } from '#/index';

export const TEST_IDENTITY: SpiderByteHostIdentity = {
  productName: 'spiderbyte-cli',
  version: '0.0.0-test',
  platform: 'spiderbyte_cli',
};
