# Deprecated legacy ACP adapter

`@spiderbyte/legacy-acp-adapter` is retained only as migration evidence for
the retired v1 SDK adapter. It is outside the workspace and Open Core graph.

The supported ACP implementation is `@spiderbyte/acp-server`, backed directly
by the canonical SpiderByte Agent Core runtime. Do not add new dependencies on
this directory.
