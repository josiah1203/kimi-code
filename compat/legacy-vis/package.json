{
  "name": "@spiderbyte/vis",
  "version": "0.1.1",
  "private": true,
  "description": "Debug visualization tool for kimi-code sessions",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:deps": "pnpm --filter @spiderbyte/vis-server... build",
    "predev": "pnpm run build:deps",
    "dev": "node scripts/dev.mjs",
    "build": "pnpm run build:deps && pnpm --filter @spiderbyte/vis-server build && pnpm --filter @spiderbyte/vis-web build && node scripts/copy-web-dist.mjs",
    "prestart": "pnpm run build",
    "start": "node server/dist/server.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
