---
"@spiderbyte/cli": minor
"@spiderbyte/sdk": minor
---

Add governed provider, dataset, ML, pipeline, and serving operations to the experimental SpiderByte platform services.
Use `/provider platform`, `/runs`, or conversational dataset/ML requests to configure connections, create Runs, transform data, train models, and inspect artifacts.
