---
"@spiderbyte/cli": minor
---

Make SpiderByte the default CLI/TUI product surface with governed provider setup, one-shot Runs, workspace inspection, and explicit compatibility and rollback paths. Use `spyderbyte configure`, `spyderbyte run`, or the new platform slash commands.
